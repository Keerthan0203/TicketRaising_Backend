function handleSearch(event) {
    event.preventDefault();
    // Perform your search logic here
    console.log("Search triggered!");
    // You can add additional logic for handling the search, e.g., redirecting to a search results page.
}